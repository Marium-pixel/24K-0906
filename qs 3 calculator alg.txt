input the numbers
read the numbers
characters are:
+,-,*,/,%
input the characters
read the characters
if the character is not from the list of characters recognized
print " enter a simpler character"
else calculate the number and character entered
read the answer
//eg if 7+3*4 is entered, answer is 19//
print the answer
end
